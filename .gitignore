
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Everything pact or an agent writes under .pact/ is local runtime state,
# EXCEPT the append-only logs and the linted plan snapshot below, which are
# history and belong in git.
.pact/*
!.pact/events.jsonl
!.pact/messages.jsonl
!.pact/plan.json


# Added by cargo

/target

# per-agent worktree checkouts (transient, merged branches are the record)
.claude/worktrees/
testdata/
